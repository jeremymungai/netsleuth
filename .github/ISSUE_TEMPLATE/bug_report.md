---
name: Bug report
about: Something went wrong analyzing a capture
labels: bug
---

**What happened?** (command, actual output, expected output)

```
$ netsleuth ...
```

**Environment**
- NetSleuth version: `netsleuth --version`
- Python version:
- OS:

**The capture** (if shareable): size, where from, and whether a
`--max-packets 200` excerpt reproduces it. If the reader misbehaves,
paste the `netsleuth summary` output including notes.

**Other context** — custom rules in play? Was `extract --output` used?
